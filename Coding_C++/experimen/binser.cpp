#include <bits/stdc++.h>
using namespace std;

int binser(vector<int>a, int n){
    int r = a.size() - 1;
    int l = 0;
    while(l < r){
        int mid = (l+r)/2;
        if(a[mid] > ){
            r = mid - 1;
        }else{
            l = mid + 1;
        }
    }

}

int main(){

}